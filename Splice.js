
// Splice will elements from an array

// splice(1,2) The first element of splice is the index value and the second is the no of values you want to remove the third element of splice will add to array

let data = [4,3,2,6,7];
console.log(data.splice(1,2,00));

console.log(data);